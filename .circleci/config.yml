version: 2
jobs:
  build:
    machine: true

    steps:
        - checkout
        - run: ./setup-dev.sh
        - run: docker-compose up -d
        - run: docker exec bat_server_1 sh -c "./run-tests.sh"
